# AGENTS.md - Qoder Memory 管理平台项目指南

> **适用范围**：本仓库（`Qoder-Memory-Admin`）的所有 AI 编程助手与自动化代理。  
> **真相源层级**：本文件继承用户级全局规则（`~/.agents/AGENTS.md`），并定义本工程专属的技术架构红线与 Git 提交约束。

---

## 1. 项目定位与核心架构

**Qoder Memory 工业级记忆拓扑管理平台**是专为 Qoder / AI Agent 研发记忆规约、避坑事实与认知拓扑打造的高性能、跨机便携原生管理系统。

### 核心技术栈与架构红线

| 模块 | 技术选型 | 架构约束与红线 |
| :--- | :--- | :--- |
| **后端服务** (`server/`) | 原生 Node.js 18+ ESM (`server.mjs`) | **零外部 npm 依赖红线**：仅使用 Node.js 内置模块（`node:http`、`node:fs`、`node:path`、`node:os` 等），严禁随意引入第三方 npm 运行时包。 |
| **前端应用** (`web/`) | 原生 HTML5 + 模块化原生 CSS3 + 原生 ES Modules | **零构建零打包**：纯 Vanilla JS + 模块化 CSS，直接浏览器原生加载运行，禁止引入 Webpack/Vite 等重型打包链破坏便携性。 |
| **核心特性** | 3D 认知引力场拓扑 (Canvas) + 知识卡片流 + 双模抽屉 | 保证 60fps 渲染流畅度、手势缩放拾取稳定性与 HUD 响应速度。 |
| **数据持久化** | 本地物理文件扫描与 Markdown 原子落盘 | 零软链接依赖，动态路径自愈；落盘时必须保持原子写入并同步重构 `MEMORY.md` 索引。 |

---

## 2. 🚨 强制 Git 提交约束（核心红线）

为确保代码版本追溯性与工作区干净，所有 AI 助手在本项目执行任务时，必须严格执行以下 **Git 提交硬性约束**：

### 2.1 每次修改代码后默认必须 Commit + Push 入库与同步
- **双重同步闭环（Commit + Push）**：**凡在本项目中修改、新增、重构或删除了代码、文档或配置，在修改与自查验证完成后，默认必须立即执行 `git add`、规范提交（`git commit`），并直接推送到远端仓库（`git push`）**。
- **杜绝脏工作区与滞后提交**：严禁在未提交代码或未同步远端的情况下结束会话或向老板交付结果。
- **状态检查**：收尾前必须主动执行 `git status` 确认工作区已 clean，且本地分支与远端保持完全同步（无未推送的 commit）。

### 2.2 提交信息规范 (Conventional Commits)
Git commit message 统一遵循 Conventional Commits 规范，且**描述必须使用中文**：

- **格式**：`<type>(<scope>): <中文简述>`
- **常用 Type**：
  - `feat`: 新增功能（如新增视图模式、接口、组件）
  - `fix`: 修复缺陷或兼容性 Bug
  - `docs`: 文档变动（如 README、AGENTS.md 说明）
  - `style`: 代码格式或样式微调（不影响逻辑）
  - `refactor`: 重构或模块优化（既不新增功能也不修 Bug）
  - `perf`: 性能提升（如 Canvas 渲染优化、扫描加速）
  - `chore`: 构建配置、脚本或辅助工具变动
- **示例**：
  - `feat(galaxy): 新增天体轨道粒子连线高亮交互`
  - `fix(server): 修复动态路径在 Windows 盘符根目录下的解析异常`
  - `docs(agents): 初始化仓库 AGENTS.md 规则与 Git 提交约束`

### 2.3 提交与推送红线
- 临时文件、排查日志、调试测试脚本严禁提交入库（依托 `.gitignore` 过滤）。
- 提交必须保持原子性与完整性，逻辑相关的改动一次性提交。
- **默认自动推送**：无需等待额外询问，commit 之后默认直接执行 `git push`；若遇网络波动或远端保护分支拦截等客观异常，在最终说明中如实汇报。

---

## 3. 研发流程与行为准则

必须严格遵循用户级全局规范（`my-code-sop` 与 `~/.agents/AGENTS.md`）：

1. **中文交互与称呼**：始终称呼“老板”，所有新增注释、日志、提交说明一律使用中文。
2. **4W 决策链**：
   - 简单改动 / 老板明确指令：内部自查清晰后**直接开始，严禁输出多余文档**；
   - 存疑或重大方案变动：仅在对话中扼要简述意图与影响面，等老板确认。
3. **偏差即停**：发现代码或业务现状与描述不符、存在未决歧义时，立即停止写入并向老板核实。
4. **零临时产物留存**：临时测试代码用完即删，服务进程退出或明确交代。

---

## 4. 常用启动与运维指令

| 指令 / 操作 | 说明 |
| :--- | :--- |
| **`start.bat`** | Windows 桌面环境首选一键启动脚本（自动打开浏览器） |
| **`npm start`** | 命令行启动后端服务（默认端口 8901） |
| **`npm run dev`** | 使用 Node 原生 `--watch` 监听热重载启动 |
| **`git status`** | 检查当前工作区未同步状态 |
